import { NotProfileDirective } from './not-profile.directive';

describe('NotProfileDirective', () => {
  it('should create an instance', () => {
    const directive = new NotProfileDirective();
    expect(directive).toBeTruthy();
  });
});
