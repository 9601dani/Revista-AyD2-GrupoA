import { NotLogoDirective } from './not-logo.directive';

describe('NotLogoDirective', () => {
  it('should create an instance', () => {
    const directive = new NotLogoDirective();
    expect(directive).toBeTruthy();
  });
});
