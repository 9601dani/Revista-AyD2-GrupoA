import { PreviewDocumentPipe } from './preview-document.pipe';

describe('PreviewDocumentPipe', () => {
  it('create an instance', () => {
    const pipe = new PreviewDocumentPipe();
    expect(pipe).toBeTruthy();
  });
});
